- hosts: all
  gather_facts: false
  tasks:
    - name: Test network zones
      debug:
        var: network_zones

    - name: Test firewall log level
      debug:
        var: firewall_log_level
- name: Deploy full stack
  hosts: all
  become: true

  roles:
    - docker
    - nginx_service
    - security